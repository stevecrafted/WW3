version: '3.7'

services:
    flight:
        image: php:8-alpine
        working_dir: /var/www
        command: php -S 0.0.0.0:8086 -t public
        environment:
            docker: "true"
        ports:
            - "8086:8080"
        volumes:
            - .:/var/www
